# Required: Postgres connection URI. Get from Supabase: Project Settings → Database → Connection string (URI).
# Port must be a NUMBER (e.g. 5432) — never use the literal word "port" or "host:port".
# If the password has special characters, URL-encode them (e.g. @ → %40).
# Example (Supabase): postgresql://postgres.[ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres
# Example (local):    postgresql://user:password@localhost:5432/mydb
# Paste your real Postgres URI here (leave no placeholder text).
# Valid example: postgresql://postgres.xxxx:YOUR_PASSWORD@aws-0-us-east-1.pooler.supabase.com:5432/postgres
DATABASE_URL=

# Required: Supabase JWT secret (used to verify access tokens). Get from Supabase: Project Settings → API → JWT Secret.
SUPABASE_JWT_SECRET=

# CORS: allowed frontend origin(s), comma-separated.
# Local:
FRONTEND_ORIGIN=http://localhost:3000,http://127.0.0.1:3000
# Production (Recruiter.Solutions):
# FRONTEND_ORIGIN=https://recruiter.solutions,https://www.recruiter.solutions

# Optional: AI, billing, job aggregation
# AI_PROVIDER=openai
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# RAPIDAPI_KEY=
# ADZUNA_APP_ID=
# ADZUNA_APP_KEY=
